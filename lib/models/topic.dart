import 'package:flutter/material.dart';

class Topic {
  String? name;
  String? imageUrl;
  Color? backgroundColor;
  IconData? icon;

  Topic({this.name, this.imageUrl, this.backgroundColor, this.icon});
}
